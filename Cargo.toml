[package]
name = "crownshell"
version = "0.3.0"
edition = "2024"
rust-version = "1.88"
authors = ["marvelxcodes <ramakrishnan0046@gmail.com>"]
description = "A Rust framework for building Wayland layer shell surfaces with Vello 2D graphics."
license = "MIT"
repository = "https://github.com/Crown-OS/crownshell"
homepage = "https://github.com/Crown-OS/crownshell"
documentation = "https://docs.rs/crownshell"
readme = "README.md"
keywords = ["crownos", "wayland", "vello", "gui", "layershell"]
categories = ["gui", "graphics", "rendering", "os::linux-apis"]

[dependencies]
anyhow = "1.0.104"
log = "0.4"

smithay-client-toolkit = { version = "0.19", features = ["calloop"] }
wayland-client = "0.31"
wayland-backend = { version = "0.3", features = ["client_system"] }
wayland-protocols = { version = "0.32", features = ["client", "staging"] }
calloop = "0.13"
calloop-wayland-source = "0.3"

vello = "0.9.0"
parley = "0.6"
pollster = "0.4"
bytemuck = { version = "1", features = ["derive"] }

# Used only by examples/. Declaring them as normal dependencies made every
# consumer of crownshell link them.
[dev-dependencies]
chrono = "0.4.45"
env_logger = "0.11"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
